# TaskManager Pro

A simple Python-based task management utility. 

## Setup
Run the following to install requirements:
`pip install -r requirements.txt`

## Usage
Run the main script to start managing tasks:
`python src/main.py --help`

## Features
- Add tasks
- List tasks
- Complete tasks (API only)

TODO: Add deployment instructions and database migration guide.
